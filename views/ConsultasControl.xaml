<UserControl x:Class="HistoriaClinicaApp.Views.ConsultasControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Button Grid.Row="0" 
                Content="➕ Nueva Consulta" 
                Command="{Binding NuevaConsultaCommand}"
                Background="#007ACC"
                Foreground="White"
                Padding="15,8"
                HorizontalAlignment="Left"
                Margin="0,0,0,10"/>
        
        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding Consultas}"
                  AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Fecha" Binding="{Binding FechaHora, StringFormat=dd/MM/yyyy HH:mm}" Width="130"/>
                <DataGridTextColumn Header="Motivo" Binding="{Binding Motivo}" Width="*"/>
                <DataGridTextColumn Header="Diagnóstico" Binding="{Binding Diagnostico}" Width="*"/>
                <DataGridTextColumn Header="Médico" Binding="{Binding NombreUsuario}" Width="120"/>
                
                <DataGridTemplateColumn Header="Acciones" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="👁️ Ver" 
                                   Command="{Binding DataContext.VerConsultaCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                   CommandParameter="{Binding}"
                                   Background="#17A2B8"
                                   Foreground="White"
                                   Padding="10,5"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>