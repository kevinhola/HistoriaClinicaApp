<UserControl x:Class="HistoriaClinicaApp.Views.EstudiosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Título -->
        <TextBlock Grid.Row="0" 
                   Text="Estudios y Laboratorios" 
                   FontSize="24" 
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>
        
        <!-- Info y Botones -->
        <Border Grid.Row="1" 
                Background="#F5F5F5" 
                Padding="15" 
                Margin="0,0,0,10"
                CornerRadius="5">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Paciente: " FontWeight="Bold"/>
                <TextBlock Text="{Binding NombrePaciente}" Margin="5,0,20,0"/>
                <TextBlock Text="DNI: " FontWeight="Bold"/>
                <TextBlock Text="{Binding DNIPaciente}" Margin="5,0,20,0"/>
                <Button Content="➕ Nuevo Estudio" 
                       Command="{Binding NuevoEstudioCommand}"
                       Background="#007ACC"
                       Foreground="White"
                       Padding="15,5"/>
            </StackPanel>
        </Border>
        
        <!-- Lista de Estudios -->
        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Estudios}"
                  SelectedItem="{Binding EstudioSeleccionado}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Fecha Solicitud" Binding="{Binding FechaSolicitud, StringFormat=dd/MM/yyyy}" Width="120"/>
                <DataGridTextColumn Header="Tipo" Binding="{Binding TipoEstudio}" Width="*"/>
                <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}" Width="*"/>
                <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="100"/>
                <DataGridTextColumn Header="Fecha Resultado" Binding="{Binding FechaResultado, StringFormat=dd/MM/yyyy}" Width="120"/>
                
                <DataGridTemplateColumn Header="Acciones" Width="250">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="✏️ Editar" 
                                       Command="{Binding DataContext.EditarEstudioCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                       CommandParameter="{Binding}"
                                       Background="#007ACC"
                                       Foreground="White"
                                       Padding="8,4"
                                       Margin="2"/>
                                <Button Content="✅ Completar" 
                                       Command="{Binding DataContext.CompletarEstudioCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                       CommandParameter="{Binding}"
                                       Background="#28A745"
                                       Foreground="White"
                                       Padding="8,4"
                                       Margin="2"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>