<UserControl x:Class="HistoriaClinicaApp.Views.ConsultasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Título -->
        <TextBlock Grid.Row="0" 
                   Text="Consultas Médicas" 
                   FontSize="24" 
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>
        
        <!-- Info Paciente -->
        <Border Grid.Row="1" 
                Background="#F5F5F5" 
                Padding="15" 
                Margin="0,0,0,10"
                CornerRadius="5">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Paciente: " FontWeight="Bold"/>
                <TextBlock Text="{Binding NombrePaciente}" Margin="5,0,20,0"/>
                <TextBlock Text="DNI: " FontWeight="Bold"/>
                <TextBlock Text="{Binding DNIPaciente}" Margin="5,0,20,0"/>
                <Button Content="➕ Nueva Consulta" 
                       Command="{Binding NuevaConsultaCommand}"
                       Background="#007ACC"
                       Foreground="White"
                       Padding="15,5"/>
            </StackPanel>
        </Border>
        
        <!-- Lista de Consultas -->
        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Consultas}"
                  SelectedItem="{Binding ConsultaSeleccionada}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Fecha" Binding="{Binding FechaHora, StringFormat=dd/MM/yyyy HH:mm}" Width="140"/>
                <DataGridTextColumn Header="Motivo" Binding="{Binding Motivo}" Width="*"/>
                <DataGridTextColumn Header="Diagnóstico" Binding="{Binding Diagnostico}" Width="*"/>
                <DataGridTextColumn Header="Médico" Binding="{Binding Usuario.NombreUsuario}" Width="120"/>
                
                <DataGridTemplateColumn Header="Acciones" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="👁️ Ver" 
                                       Command="{Binding DataContext.VerConsultaCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                       CommandParameter="{Binding}"
                                       Background="#17A2B8"
                                       Foreground="White"
                                       Padding="8,4"
                                       Margin="2"/>
                                <Button Content="📝 Receta" 
                                       Command="{Binding DataContext.RecetasCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                       CommandParameter="{Binding}"
                                       Background="#28A745"
                                       Foreground="White"
                                       Padding="8,4"
                                       Margin="2"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>