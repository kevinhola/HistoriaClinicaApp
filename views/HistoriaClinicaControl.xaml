<UserControl x:Class="HistoriaClinicaApp.Views.HistoriaClinicaControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="15">
            <TextBlock Text="Antecedentes Médicos:" FontWeight="Bold" FontSize="14" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Historia.Antecedentes, UpdateSourceTrigger=PropertyChanged}" 
                     TextWrapping="Wrap" 
                     AcceptsReturn="True"
                     MinHeight="80"
                     VerticalScrollBarVisibility="Auto"/>
            
            <TextBlock Text="Alergias:" FontWeight="Bold" FontSize="14" Margin="0,15,0,5"/>
            <TextBox Text="{Binding Historia.Alergias, UpdateSourceTrigger=PropertyChanged}" 
                     TextWrapping="Wrap" 
                     AcceptsReturn="True"
                     MinHeight="60"/>
            
            <TextBlock Text="Medicación Habitual:" FontWeight="Bold" FontSize="14" Margin="0,15,0,5"/>
            <TextBox Text="{Binding Historia.MedicacionHabitual, UpdateSourceTrigger=PropertyChanged}" 
                     TextWrapping="Wrap" 
                     AcceptsReturn="True"
                     MinHeight="60"/>
            
            <TextBlock Text="Grupo Sanguíneo:" FontWeight="Bold" FontSize="14" Margin="0,15,0,5"/>
            <ComboBox SelectedItem="{Binding Historia.GrupoSanguineo}" Width="150" HorizontalAlignment="Left">
                <ComboBoxItem Content="A+"/>
                <ComboBoxItem Content="A-"/>
                <ComboBoxItem Content="B+"/>
                <ComboBoxItem Content="B-"/>
                <ComboBoxItem Content="AB+"/>
                <ComboBoxItem Content="AB-"/>
                <ComboBoxItem Content="O+"/>
                <ComboBoxItem Content="O-"/>
            </ComboBox>
            
            <TextBlock Text="Observaciones:" FontWeight="Bold" FontSize="14" Margin="0,15,0,5"/>
            <TextBox Text="{Binding Historia.Observaciones, UpdateSourceTrigger=PropertyChanged}" 
                     TextWrapping="Wrap" 
                     AcceptsReturn="True"
                     MinHeight="80"/>
            
            <Button Content="💾 Guardar Historia Clínica" 
                   Command="{Binding GuardarCommand}"
                   Background="#28A745"
                   Foreground="White"
                   HorizontalAlignment="Left"
                   Margin="0,20,0,0"
                   Padding="20,10"
                   FontSize="14"
                   FontWeight="Bold"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>