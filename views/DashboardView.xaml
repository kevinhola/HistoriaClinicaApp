<UserControl x:Class="HistoriaClinicaApp.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#007ACC">
            <Grid Margin="25,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🏥 " FontSize="24"/>
                    <TextBlock Text="Sistema de Historia Clínica Electrónica" 
                              FontSize="22" 
                              FontWeight="Bold"
                              Foreground="White"
                              VerticalAlignment="Center"/>
                </StackPanel>
                
                <StackPanel Orientation="Horizontal" 
                           HorizontalAlignment="Right" 
                           VerticalAlignment="Center">
                    <TextBlock Text="{Binding UsuarioActual}" 
                              Foreground="White" 
                              FontSize="14"
                              FontWeight="SemiBold"
                              VerticalAlignment="Center"
                              Margin="0,0,15,0"/>
                    <TextBlock Text="|" 
                              Foreground="White" 
                              VerticalAlignment="Center"
                              Margin="0,0,15,0"/>
                    <TextBlock Text="{Binding RolActual}" 
                              Foreground="White" 
                              FontSize="14"
                              VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Contenido -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Menu Lateral -->
            <Border Grid.Column="0" Background="#2D2D30" BorderBrush="#3E3E42" BorderThickness="0,0,1,0">
                <StackPanel Margin="0,25,0,0">
                    <Button Content="📊 Inicio" 
                           Command="{Binding NavInicioCommand}"
                           Style="{StaticResource MenuButtonStyle}"/>
                    
                    <Button Content="👥 Pacientes" 
                           Command="{Binding NavPacientesCommand}"
                           Style="{StaticResource MenuButtonStyle}"/>
                    
                    <Button Content="⚙️ Administración" 
                           Command="{Binding NavAdminCommand}"
                           Style="{StaticResource MenuButtonStyle}"
                           Visibility="{Binding EsAdmin, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    
                    <Button Content="👤 Mi Perfil" 
                           Command="{Binding NavPerfilCommand}"
                           Style="{StaticResource MenuButtonStyle}"
                           Margin="0,20,0,0"/>
                    
                    <Separator Margin="15,25" Background="#3E3E42"/>
                    
                    <Button Content="🚪 Cerrar Sesión" 
                           Command="{Binding CerrarSesionCommand}"
                           Background="Transparent"
                           Foreground="#FF6B6B"
                           HorizontalAlignment="Stretch"
                           HorizontalContentAlignment="Left"
                           Padding="20,12"
                           BorderThickness="0"
                           FontSize="14"
                           Cursor="Hand"/>
                </StackPanel>
            </Border>
            
            <!-- Area de Contenido -->
            <ContentControl Grid.Column="1" x:Name="DashboardContent" Margin="25"/>
        </Grid>
    </Grid>
</UserControl>